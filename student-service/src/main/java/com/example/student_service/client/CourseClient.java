         package com.example.student_service.client;

			/*
			 * import org.springframework.cloud.openfeign.FeignClient; import
			 * org.springframework.web.bind.annotation.GetMapping; import
			 * org.springframework.web.bind.annotation.PathVariable;
			 * 
			 * import com.example.course_service.entity.Course;
			 */

/*
 * @FeignClient(name = "course-service") public interface CourseClient {
 * 
 * @GetMapping("/courses/{id}") Course getCourseById(@PathVariable Long id); }
 */


